#include "../../headers/includes.hpp"


// Idea:
// Each entity has an assigned pipeline to run it through?
// Make a function like "->Render()", where it utilizes that pointed-to member pipeline
// Then we just have to bind vertex and index buffers

// But what about descriptors and command buffers? How does a proper game engine handle descriptors and command buffers?
// Is it just like we have to make members for descriptors?


struct RenderEntity
{

};